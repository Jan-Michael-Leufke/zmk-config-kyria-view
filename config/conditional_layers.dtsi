// filepath: d: \zmk\zmk-config-kyria-view\config\conditional_layers.dtsi
// / {
    //     conditional_layers {
        //         compatible = "zmk,conditional-layers";
        //         nav_move_chord {
            //             if-layers = <1>;      // nav layer (adjust if needed)
            //             if-keys = <12>;       // LCTRL position (adjust to your layout)
            //             then-layer = <3>;     // move layer (adjust if needed)
        //         };
    //     };
// };