// // filepath: d: \zmk\zmk-config-kyria-view\config\behaviors.dtsi

// #include <dt-bindings/zmk/behaviors.h>
// #include <dt-bindings/zmk/keys.h>
// / {
    //     behaviors {
        //         my_lt: my_layer_tap {
            //             compatible = "zmk,behavior-hold-tap";
            //             #binding-cells = <2>;
            //             flavor = "tap-preferred";         // or "hold-preferred", etc.
            //             tapping-term-ms = <200>;
            //             bindings = <&mo>, <&kp>;
            //             display-name = "My Layer Tap";
        //         };
        //         my_mt: my_mod_tap {
            //             compatible = "zmk,behavior-hold-tap";
            //             #binding-cells = <2>;
            //             flavor = "hold-preferred";
            //             tapping-term-ms = <200>;
            //             bindings = <&kp>, <&kp>;
            //             display-name = "My Mod Tap";
        //         };

        //         hml: home_row_mod_left {
            //             compatible = "zmk,behavior-hold-tap";
            //             #binding-cells = <2>;
            //             flavor = "balanced";
            //             require-prior-idle-ms = <150>;
            //             tapping-term-ms = <280>;
            //             quick-tap-ms = <175>;
            //             bindings = <&kp>, <&kp>;
            //             hold-trigger-key-positions = <...>; // List of keys on the right side of the keyboard
            //             hold-trigger-on-release;
        //         };
        //         hmr: home_row_mod_right {
            //             compatible = "zmk,behavior-hold-tap";
            //             #binding-cells = <2>;
            //             flavor = "balanced";
            //             require-prior-idle-ms = <150>;
            //             tapping-term-ms = <280>;
            //             quick-tap-ms = <175>;
            //             bindings = <&kp>, <&kp>;
            //             hold-trigger-key-positions = <...>; // List of keys on the left side of the keyboard
            //             hold-trigger-on-release;
        //         };
    //     };
// };